# --- 核心深度学习框架 ---
torch>=2.1.0
torchvision>=0.16.0
torchaudio

# --- Mamba 架构核心 (SSM) ---
# 注意：这两个库通常需要 GPU 环境进行编译
causal-conv1d>=1.2.0
mamba-ssm>=1.2.0

# --- Web 前端与交互 ---
streamlit>=1.30.0

# --- 图像与视频处理 ---
# 建议使用 headless 版本以减少服务器 GUI 依赖冲突
opencv-python-headless
Pillow>=10.0.0
einops>=0.7.0

# --- 数据处理与指标计算 ---
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
tqdm

# --- 可视化 ---
matplotlib
